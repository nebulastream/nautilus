{
	"name": "nautilus-playground-server",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "API server for the Nautilus playground: queues compile jobs and runs them in sandboxed runner containers.",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "tsc && mkdir -p dist/examples && cp src/examples/*.cpp dist/examples/",
		"dev": "tsx watch src/index.ts",
		"start": "node dist/index.js",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@fastify/cors": "^10.0.0",
		"@fastify/static": "^8.0.0",
		"fastify": "^5.0.0",
		"zod": "^3.23.0"
	},
	"devDependencies": {
		"@types/node": "^20.14.0",
		"tsx": "^4.16.0",
		"typescript": "^5.5.0"
	}
}
